cd ~/Downloads          # or wherever both files are
git init
git add password_strength_checker.py README.md
git commit -m "Add Project 1: Password Strength Checker"
git remote add origin https://github.com/yourusername/password-strength-checker.git
git branch -M main
git push -u origin main
